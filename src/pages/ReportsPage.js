import React from "react";
import Typography from "@mui/material/Typography";

import T from "T";

const ReportsPage = () => {
  return <Typography variant="h4">{`${T.REPORTS} ${T.PAGE}`}</Typography>;
};

export default ReportsPage;
